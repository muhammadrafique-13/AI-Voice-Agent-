# Runtime
fastapi==0.115.6
pydantic==2.10.4
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Local development only (Vercel's Python runtime provides its own ASGI server).
# Plain uvicorn, not uvicorn[standard]: the extras add Rust-built native deps that
# slow installs and are never used on serverless.
uvicorn==0.34.0
python-dotenv==1.0.1
